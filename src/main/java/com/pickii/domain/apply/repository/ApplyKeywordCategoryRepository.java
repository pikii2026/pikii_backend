package com.pickii.domain.apply.repository;

import com.pickii.domain.apply.entity.ApplyKeywordCategory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApplyKeywordCategoryRepository extends JpaRepository<ApplyKeywordCategory, Long> {
}
